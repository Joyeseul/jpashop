spring:
#  스프링부트는 별도의 설정이 없으면 test 는 메모리 DB 모드로 돌린다..!
#  datasource:
#    url: jdbc:h2:mem:testdb   #메모리 DB를 사용하게 함
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace